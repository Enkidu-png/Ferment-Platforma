---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified: []
autonomous: false
requirements:
  - FOUN-02
  - FOUN-03
  - FOUN-04

must_haves:
  truths:
    - "All 8 tables exist in Supabase PostgreSQL (tenants, users, user_tenants, categories, tags, products, product_tags, orders, reviews, media)"
    - "The tenants table has a status column with check constraint (pending, approved, rejected, suspended)"
    - "Row Level Security is enabled on all 10 tables"
    - "Unauthenticated buyers can read products and tenants only when tenant status = approved"
    - "Products from pending or suspended merchants are invisible to anonymous queries"
    - "Tenant owners can read and write only their own data (enforced at DB level)"
    - "The auth.tenant_id() and auth.is_super_admin() helper functions exist in the database"
  artifacts:
    - path: "Supabase DB: public.tenants"
      provides: "Merchant identity with status lifecycle"
      contains: "status text not null default 'pending' check (status in ('pending','approved','rejected','suspended'))"
    - path: "Supabase DB: public.products"
      provides: "Product catalog with tenant FK"
      contains: "tenant_id uuid not null references tenants(id)"
    - path: "Supabase DB: public.user_tenants"
      provides: "Merchant membership junction (user -> tenant)"
    - path: "Supabase DB: public.orders"
      provides: "Purchase records"
    - path: "Supabase DB: public.reviews"
      provides: "Product reviews"
    - path: "Supabase DB: public.media"
      provides: "File metadata (Supabase Storage paths)"
    - path: "Supabase DB: RLS policies on products"
      provides: "Public read filtered by tenant.status = approved"
  key_links:
    - from: "products public-read policy"
      to: "tenants.status"
      via: "EXISTS subquery in policy expression"
      pattern: "exists.*select 1 from tenants t.*t.status = 'approved'"
    - from: "auth.tenant_id() function"
      to: "auth.jwt()"
      via: "nullif cast pattern for null safety"
      pattern: "nullif.*auth.jwt.*tenant_id.*null.*::uuid"
---

<objective>
Execute all PostgreSQL schema SQL and RLS policy SQL in the Supabase SQL editor. This creates all 8 tables (10 including junction tables), enables Row Level Security on every table, and deploys the complete set of security policies that enforce tenant isolation and public visibility rules.

Purpose: Without this SQL, no data can be stored and no security guarantees exist. All subsequent phases write data to these tables and rely on the RLS policies for tenant isolation.
Output: 10 tables in Supabase PostgreSQL with indexes, RLS enabled, and a complete set of security policies covering all operations (SELECT/INSERT/UPDATE/DELETE) for all roles (anon, authenticated tenant owner, super-admin).
</objective>

<execution_context>
@C:/Users/Sasze/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Sasze/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Run Schema SQL — Create All 10 Tables with Indexes</name>
  <files></files>
  <action>
    Run the complete table creation SQL block in the Supabase SQL Editor. All tables must be created before RLS policies are applied in Task 2.

    Steps:
    1. Go to your Supabase project dashboard.
    2. In the left sidebar, click "SQL Editor".
    3. Click "New query".
    4. Paste the ENTIRE SQL block below into the editor (run it as one block, not in parts).
    5. Click "Run" (or press Ctrl+Enter).
    6. Go to Database > Tables and confirm all 10 tables are visible:
       media, tenants, users, user_tenants, categories, tags, products, product_tags, orders, reviews

    SQL TO RUN:

    -- MEDIA (no FKs; referenced by tenants and products)
    create table media (
      id           uuid primary key default gen_random_uuid(),
      alt          text not null,
      url          text not null,
      storage_path text not null,
      width        integer,
      height       integer,
      mime_type    text,
      created_at   timestamptz not null default now()
    );

    -- TENANTS
    -- CRITICAL: status column gates public visibility via RLS -- do not remove
    create table tenants (
      id                       uuid primary key default gen_random_uuid(),
      name                     text not null,
      slug                     text not null unique,
      status                   text not null default 'pending'
                               check (status in ('pending', 'approved', 'rejected', 'suspended')),
      stripe_account_id        text not null unique,
      stripe_details_submitted boolean not null default false,
      image_id                 uuid references media(id),
      created_at               timestamptz not null default now(),
      updated_at               timestamptz not null default now()
    );
    create index on tenants(slug);
    create index on tenants(status);

    -- USERS (profile extending auth.users)
    create table users (
      id          uuid primary key references auth.users(id) on delete cascade,
      username    text not null unique,
      role        text not null default 'user' check (role in ('user', 'super-admin')),
      created_at  timestamptz not null default now(),
      updated_at  timestamptz not null default now()
    );

    -- USER_TENANTS (merchant membership junction)
    create table user_tenants (
      user_id   uuid not null references users(id) on delete cascade,
      tenant_id uuid not null references tenants(id) on delete cascade,
      primary key (user_id, tenant_id)
    );
    create index on user_tenants(user_id);
    create index on user_tenants(tenant_id);

    -- CATEGORIES
    create table categories (
      id         uuid primary key default gen_random_uuid(),
      name       text not null,
      slug       text not null unique,
      color      text,
      parent_id  uuid references categories(id),
      created_at timestamptz not null default now()
    );
    create index on categories(parent_id);

    -- TAGS
    create table tags (
      id         uuid primary key default gen_random_uuid(),
      name       text not null unique,
      created_at timestamptz not null default now()
    );

    -- PRODUCTS
    create table products (
      id            uuid primary key default gen_random_uuid(),
      tenant_id     uuid not null references tenants(id) on delete cascade,
      name          text not null,
      description   text,
      price         numeric(10,2) not null,
      category_id   uuid references categories(id),
      image_id      uuid references media(id),
      cover_id      uuid references media(id),
      refund_policy text not null default '30-day'
                    check (refund_policy in ('30-day','14-day','7-day','3-day','1-day','no-refunds')),
      content       text,
      is_private    boolean not null default false,
      is_archived   boolean not null default false,
      created_at    timestamptz not null default now(),
      updated_at    timestamptz not null default now()
    );
    create index on products(tenant_id);
    create index on products(category_id);
    create index on products(is_archived, is_private);
    create index on products(tenant_id, is_archived, is_private);

    -- PRODUCT_TAGS (many-to-many junction)
    create table product_tags (
      product_id uuid not null references products(id) on delete cascade,
      tag_id     uuid not null references tags(id) on delete cascade,
      primary key (product_id, tag_id)
    );

    -- ORDERS
    create table orders (
      id                         uuid primary key default gen_random_uuid(),
      user_id                    uuid not null references users(id),
      product_id                 uuid not null references products(id),
      stripe_checkout_session_id text not null unique,
      stripe_account_id          text,
      created_at                 timestamptz not null default now()
    );
    create index on orders(user_id);
    create index on orders(product_id);

    -- REVIEWS
    create table reviews (
      id          uuid primary key default gen_random_uuid(),
      product_id  uuid not null references products(id) on delete cascade,
      user_id     uuid not null references users(id),
      description text not null,
      rating      smallint not null check (rating between 1 and 5),
      created_at  timestamptz not null default now(),
      unique (product_id, user_id)
    );
  </action>
  <verify>
    <manual>
      In Supabase dashboard: Database > Tables shows 10 tables:
      media, tenants, users, user_tenants, categories, tags, products, product_tags, orders, reviews.
      Check tenants table has a "status" column: Database > Tables > tenants > click to view columns.
    </manual>
  </verify>
  <done>All 10 tables visible in Database > Tables. The tenants table has a status column with default value 'pending'. SQL ran without errors.</done>
  <what-built>Claude prepared the complete schema SQL. You are running it in the Supabase SQL editor.</what-built>
  <how-to-verify>See action steps above.</how-to-verify>
  <resume-signal>Type "schema created" after confirming all 10 tables are visible in the Supabase dashboard</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Run Helper Functions and RLS Policies SQL</name>
  <files></files>
  <action>
    Run the helper functions and RLS policy SQL in a new query tab. Tables must already exist from Task 1.

    Steps:
    1. In the Supabase SQL Editor, click "New query" (do not reuse the previous query tab).
    2. Paste the ENTIRE SQL block below and run it as one block.
    3. If any statement fails, the most common cause is running out of order. This SQL is ordered correctly; run it top-to-bottom without splitting.

    After running, verify by running these test queries in a NEW SQL editor tab:
      -- Test 1: helper functions return correct types
      select pg_typeof(auth.tenant_id());      -- should return "uuid"
      select pg_typeof(auth.is_super_admin()); -- should return "boolean"
      -- Test 2: anon cannot see orders (private data)
      set role anon;
      select count(*) from orders;
      reset role;
      -- should return 0 (no rows visible to anon role)

    SQL TO RUN:

    -- HELPER FUNCTIONS (used inside RLS policy expressions)
    -- Returns current user's tenant_id from JWT without extra DB query.
    -- nullif handles JSON null serialized as the string "null" for buyers without a tenant.
    create or replace function auth.tenant_id() returns uuid language sql stable as $$
      select coalesce(
        nullif((auth.jwt() ->> 'tenant_id'), 'null')::uuid,
        null
      )
    $$;

    -- Returns true if current user has app_role = 'super-admin' in their JWT
    create or replace function auth.is_super_admin() returns boolean language sql stable as $$
      select coalesce(
        (auth.jwt() ->> 'app_role') = 'super-admin',
        false
      )
    $$;

    -- ENABLE RLS ON ALL TABLES
    alter table tenants      enable row level security;
    alter table users        enable row level security;
    alter table user_tenants enable row level security;
    alter table products     enable row level security;
    alter table product_tags enable row level security;
    alter table categories   enable row level security;
    alter table tags         enable row level security;
    alter table orders       enable row level security;
    alter table reviews      enable row level security;
    alter table media        enable row level security;

    -- TENANTS POLICIES
    -- Anon sees only approved tenants (powers public storefront listing)
    create policy "tenants: anon read approved"
      on tenants for select
      using (status = 'approved');

    -- Tenant owner sees their own tenant even if pending/suspended
    create policy "tenants: owner read own"
      on tenants for select
      using (id = auth.tenant_id());

    -- Super-admin has full access to all tenants
    create policy "tenants: super-admin all"
      on tenants for all
      using (auth.is_super_admin())
      with check (auth.is_super_admin());

    -- PRODUCTS POLICIES
    -- CRITICAL: anon read checks tenant.status = 'approved' -- not just is_archived/is_private
    -- Products from pending/suspended merchants must be invisible to anonymous buyers
    create policy "products: anon read approved-tenant"
      on products for select
      using (
        is_archived = false
        and is_private = false
        and exists (
          select 1 from tenants t
          where t.id = products.tenant_id
          and t.status = 'approved'
        )
      );

    -- Tenant owner sees ALL their products (including private, archived -- for their dashboard)
    create policy "products: owner read all"
      on products for select
      using (tenant_id = auth.tenant_id());

    create policy "products: owner insert"
      on products for insert
      with check (tenant_id = auth.tenant_id());

    create policy "products: owner update"
      on products for update
      using (tenant_id = auth.tenant_id())
      with check (tenant_id = auth.tenant_id());

    create policy "products: owner delete"
      on products for delete
      using (tenant_id = auth.tenant_id());

    create policy "products: super-admin all"
      on products for all
      using (auth.is_super_admin())
      with check (auth.is_super_admin());

    -- USERS POLICIES
    create policy "users: read own"
      on users for select
      using (id = auth.uid());

    create policy "users: update own"
      on users for update
      using (id = auth.uid())
      with check (id = auth.uid());

    create policy "users: super-admin all"
      on users for all
      using (auth.is_super_admin())
      with check (auth.is_super_admin());

    -- USER_TENANTS POLICIES
    create policy "user_tenants: read own"
      on user_tenants for select
      using (user_id = auth.uid());

    create policy "user_tenants: super-admin all"
      on user_tenants for all
      using (auth.is_super_admin())
      with check (auth.is_super_admin());

    -- PRODUCT_TAGS POLICIES
    create policy "product_tags: public read"
      on product_tags for select using (true);

    create policy "product_tags: owner write"
      on product_tags for all
      using (
        exists (
          select 1 from products p
          where p.id = product_tags.product_id
          and p.tenant_id = auth.tenant_id()
        )
      )
      with check (
        exists (
          select 1 from products p
          where p.id = product_tags.product_id
          and p.tenant_id = auth.tenant_id()
        )
      );

    create policy "product_tags: super-admin all"
      on product_tags for all
      using (auth.is_super_admin())
      with check (auth.is_super_admin());

    -- ORDERS POLICIES
    -- INSERT is done by Stripe webhook via service-role client (bypasses RLS); no INSERT policy needed
    create policy "orders: user read own"
      on orders for select
      using (user_id = auth.uid());

    create policy "orders: super-admin all"
      on orders for all
      using (auth.is_super_admin())
      with check (auth.is_super_admin());

    -- REVIEWS POLICIES
    create policy "reviews: public read"
      on reviews for select using (true);

    create policy "reviews: authenticated insert"
      on reviews for insert
      with check (auth.uid() is not null and user_id = auth.uid());

    create policy "reviews: owner update"
      on reviews for update
      using (user_id = auth.uid())
      with check (user_id = auth.uid());

    create policy "reviews: owner delete"
      on reviews for delete
      using (user_id = auth.uid());

    -- CATEGORIES POLICIES
    create policy "categories: public read"
      on categories for select using (true);

    create policy "categories: super-admin write"
      on categories for all
      using (auth.is_super_admin())
      with check (auth.is_super_admin());

    -- TAGS POLICIES
    create policy "tags: public read"
      on tags for select using (true);

    create policy "tags: super-admin write"
      on tags for all
      using (auth.is_super_admin())
      with check (auth.is_super_admin());

    -- MEDIA POLICIES
    create policy "media: public read"
      on media for select using (true);

    create policy "media: authenticated insert"
      on media for insert
      with check (auth.uid() is not null);

    create policy "media: super-admin delete"
      on media for delete
      using (auth.is_super_admin());
  </action>
  <verify>
    <manual>
      1. Run: select pg_typeof(auth.tenant_id()); -- should return "uuid"
      2. Run: select pg_typeof(auth.is_super_admin()); -- should return "boolean"
      3. In Database > Tables, click any table and confirm the "RLS" badge shows "Enabled".
      4. Run: set role anon; select count(*) from orders; reset role; -- should return 0
    </manual>
  </verify>
  <done>
    All 10 tables have RLS enabled. auth.tenant_id() returns uuid type. auth.is_super_admin() returns boolean type.
    Products public-read policy contains an EXISTS subquery checking tenants.status = 'approved'.
    SQL ran without errors.
  </done>
  <what-built>Claude prepared the complete RLS SQL. You are running it in the Supabase SQL editor.</what-built>
  <how-to-verify>See action steps and test queries above.</how-to-verify>
  <resume-signal>Type "RLS deployed" after all policies are created and the test queries run without errors</resume-signal>
</task>

</tasks>

<verification>
After both tasks complete:
- Database > Tables shows 10 tables: media, tenants, users, user_tenants, categories, tags, products, product_tags, orders, reviews
- Every table has RLS badge showing "Enabled"
- Database > Functions shows auth.tenant_id() and auth.is_super_admin()
- Test query: set role anon; select count(*) from orders limit 1; reset role; returns 0 (orders are private)
- Test query: select pg_typeof(auth.tenant_id()) returns "uuid"
- The tenants table has the status column: select column_name, data_type from information_schema.columns where table_name='tenants' and column_name='status'; returns one row
</verification>

<success_criteria>
1. 10 tables created in Supabase PostgreSQL with all indexes
2. tenants.status column exists with check constraint (pending, approved, rejected, suspended)
3. RLS enabled on all 10 tables
4. auth.tenant_id() and auth.is_super_admin() functions exist in the database
5. products public-read policy includes tenant status check (pending/suspended products invisible to anon)
6. All role policies deployed: anon read for public data, owner CRUD for tenant-scoped data, super-admin full access
7. No RLS policy omissions — every table has at least one SELECT policy
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
