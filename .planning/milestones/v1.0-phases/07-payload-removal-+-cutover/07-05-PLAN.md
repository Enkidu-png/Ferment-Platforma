---
phase: 07-payload-removal-+-cutover
plan: 05
type: execute
wave: 4
depends_on:
  - 07-03
files_modified: []
autonomous: false
requirements:
  - CLEN-05
gap_closure: true

must_haves:
  truths:
    - "artist1@test.ferment.com has received a password reset email with a working reset link"
    - "All 3 seed artist accounts have received password reset emails (SENT count = 3)"
  artifacts:
    - path: "scripts/send-password-resets.ts"
      provides: "Password reset script — unchanged, already correct from Plan 03"
  key_links:
    - from: "scripts/send-password-resets.ts"
      to: "supabase.auth.resetPasswordForEmail"
      via: "service-role client — rate-limit window must have passed since last run"
      pattern: "resetPasswordForEmail"
---

<objective>
Re-send the password reset email to artist1@test.ferment.com, which was rate-limited during the Plan 03 live run. The script is correct and unchanged — this plan closes the gap by waiting for the Supabase Free tier rate-limit window to clear and re-running.

Purpose: Truth 7 — "All existing artists have received a password reset email" — is 2/3. This plan achieves 3/3.
Output: Confirmed SENT line for artist1@test.ferment.com; rate-limit window verified by checking Supabase Auth Logs.
</objective>

<execution_context>
@C:/Users/jan.sachse/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jan.sachse/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-payload-removal-+-cutover/07-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Wait for rate-limit window and re-run password reset script</name>
  <what-to-do>
The Plan 03 live run produced:
```
SENT artist3@test.ferment.com
SENT artist2@test.ferment.com
FAIL artist1@test.ferment.com: email rate limit exceeded
Done.
```

Supabase Free tier allows 4 emails per hour. The third email hit the limit. The script and configuration are correct — this is purely a timing issue.

**Step 1 — Confirm the rate-limit window has passed:**
Check Supabase Dashboard → Authentication → Logs. The last reset email send was at the time of the Plan 03 execution. At least 1 hour must have elapsed since the last successful send before re-running.

If less than 1 hour has passed since the Plan 03 run, wait until the window clears.

**Step 2 — Re-run the script in live mode:**
```bash
npx tsx --env-file=.env.local --env-file=.env scripts/send-password-resets.ts 2>&1 | tee /tmp/reset-send-output-2.txt
echo "Exit code: $?"
```

The script will attempt to send to all artist accounts (it does not track who was already sent — it re-sends to all). artist2 and artist3 will receive a second reset email (harmless). artist1 should receive their first.

**Step 3 — Confirm output:**
```bash
cat /tmp/reset-send-output-2.txt
```

Expected output:
```
Found 3 artists. Dry run: false
SENT artist3@test.ferment.com
SENT artist2@test.ferment.com
SENT artist1@test.ferment.com
Done.
```

All three lines must show `SENT`, not `FAIL`.

**Step 4 — Verify in Supabase Dashboard:**
Go to Supabase Dashboard → Authentication → Logs and confirm a new email dispatch event for `artist1@test.ferment.com`.
  </what-to-do>
  <resume-signal>Type "artist1 sent" once the output shows "SENT artist1@test.ferment.com" and the script ends with "Done." Type "still rate limited" if the window has not cleared yet and include the current timestamp.</resume-signal>
  <files></files>
  <action>Human action required: wait for Supabase Free tier rate-limit window (1 hour from last send), then re-run the password reset script and confirm SENT for all 3 artists.</action>
  <verify>
    <automated>[ -f /tmp/reset-send-output-2.txt ] && grep "SENT artist1@test.ferment.com" /tmp/reset-send-output-2.txt && echo "PASS: artist1 confirmed sent" || echo "PENDING: output file not found or artist1 not sent"</automated>
  </verify>
  <done>
- `/tmp/reset-send-output-2.txt` contains `SENT artist1@test.ferment.com`
- Script output ends with `Done.` and exit code 0
- No `FAIL` lines in the output
- Supabase Auth Logs show the email dispatch event for artist1
  </done>
</task>

</tasks>

<verification>
Truth 7 is fully verified when:

1. `/tmp/reset-send-output-2.txt` contains `SENT artist1@test.ferment.com` (not `FAIL`)
2. Script ends with `Done.` and exited 0
3. Supabase Auth Logs show 3 separate email dispatch events across the Plan 03 run and this re-run
</verification>

<success_criteria>
1. artist1@test.ferment.com receives a password reset email with the migration-specific copy and a working reset link
2. All 3 seed artist accounts (artist1, artist2, artist3) have received at least one password reset email
3. The script exits 0 on the re-run with zero FAIL lines
</success_criteria>

<output>
After completion, create `.planning/phases/07-payload-removal-+-cutover/07-05-SUMMARY.md`
</output>
