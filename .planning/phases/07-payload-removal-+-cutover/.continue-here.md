---
phase: 07-payload-removal-+-cutover
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-03-12T13:06:27.054Z
---

<current_state>
Plan 07-04 is at Task 3 (human verification checkpoint). The cookie domain fix is deployed to production via CLI (commit 0d6522f, deployment dpl_D91vfXCJXaEpfdEaEoFkYSoLRogU, READY). Admin login still needs human verification.

GitHub-triggered builds were failing with "supabaseUrl is required" because supabaseAdmin was initialized at module level in src/lib/supabase/admin.ts. Fixed with a lazy Proxy (commit d44f782, pushed to ferment/master). A new GitHub build was triggered — result not yet confirmed.

Plan 07-05 (re-send rate-limited password reset to artist1@test.ferment.com) has NOT been started yet.
</current_state>

<completed_work>

**Plan 07-01** ✓ — Payload CMS dead code removed, build passes
**Plan 07-02** ✓ — Deployed to Vercel production, Playwright smoke tests patched
**Plan 07-03** ✓ — Password reset script run live; artist2 and artist3 notified; artist1 was rate-limited

**Plan 07-04 — Tasks 1 & 2 done:**
- Task 1 (commit 88d583f): Fixed `src/lib/supabase/client.ts` — cookie domain guard
- Task 2 (commit 54dc3ed): Storefront smoke tests skip when `PLAYWRIGHT_BASE_URL` is non-localhost
- Additional fix (commit 0d6522f): Extended cookie domain guard to `middleware.ts` and `server.ts` — root cause was middleware overwriting session cookies on every request

**Infrastructure fixes this session (2026-03-12):**
- Deleted `ferment-platforma-agt4` Vercel project — was a duplicate causing double builds on every push
- Added env vars to `ferment-platforma` Vercel dashboard
- Fixed `src/lib/supabase/admin.ts` (commit d44f782): lazy Proxy initialization so build doesn't crash when env vars absent at build time
- Pushed trigger commits; latest GitHub build in progress for d44f782

**Production state:**
- Production URL: https://ferment-platforma.vercel.app
- Current prod deployment: dpl_D91vfXCJXaEpfdEaEoFkYSoLRogU (commit 0d6522f, CLI-deployed, READY)
</completed_work>

<remaining_work>

**Verify GitHub build (d44f782) before anything else:**
- Check `mcp__vercel__list_deployments` for a deployment with commit d44f782 in READY or ERROR state
- If ERROR: env vars likely set for Production scope only — user needs Vercel → Settings → Environment Variables → edit each Supabase var → tick Preview checkbox
- If READY: GitHub integration is fully working

**Plan 07-04 — Task 3 remaining:**
- Human verification: open incognito → https://ferment-platforma.vercel.app → log in with super-admin → refresh → confirm session persists → check /admin shows real data
- Run smoke suite: `PLAYWRIGHT_BASE_URL=https://ferment-platforma.vercel.app npx playwright test tests/smoke/ --reporter=line`
- Expected: 4 storefront tests skipped, others pass, exits 0
- Write 07-04-SUMMARY.md after confirmation

**Plan 07-05 — Not started:**
- Re-send password reset to artist1@test.ferment.com (was rate-limited during 07-03)
- Rate-limit window has cleared (was hit 2026-03-10, now 2026-03-12)
- Script: `npx tsx --env-file=.env.local scripts/send-password-resets.ts 2>&1 | tee /tmp/reset-send-output-2.txt`
- Expected output: all 3 lines show SENT, exit 0
- Write 07-05-SUMMARY.md after confirmation
</remaining_work>

<decisions_made>

- Extended cookie domain fix to middleware.ts and server.ts (not just client.ts) — root cause required all three files
- Used Proxy pattern for lazy supabaseAdmin instead of changing to a getter function — keeps all 6 call sites unchanged
- GitHub builds create Preview deployments (target: null), not Production — env vars must be set for Preview + Production + Development scopes in Vercel dashboard
- Deleted ferment-platforma-agt4 project — was a duplicate
- Production remains the CLI-deployed dpl_D91vfXCJXaEpfdEaEoFkYSoLRogU until GitHub builds are confirmed passing
</decisions_made>

<blockers>

- GitHub build for d44f782 result unknown — may still fail if env vars scoped to Production only (not Preview)
- Admin login human verification still pending
</blockers>

<context>
This session was mainly a build failure investigation. The production site is healthy. Two root causes fixed:
1. supabaseAdmin initialized at module level → Next.js crashed during "Collecting page data" → fixed with lazy Proxy
2. Duplicate Vercel project agt4 → user deleted it

The remaining work is small: confirm the GitHub build passes, verify admin login, run the password reset script for artist1. Phase 07 is ~97% done.
</context>

<next_action>
1. Check Vercel build result for commit d44f782: use mcp__vercel__list_deployments and look for READY or ERROR.
2. If READY → GitHub integration fixed. Ask user to verify admin login.
3. If ERROR → ask user to set env vars for Preview scope in Vercel dashboard, then retrigger.
4. After admin verified → run 07-05 password reset script.
</next_action>
